# VS Code Settings (Backup Only)

These files are **not installed** by `install.sh` and **not stowed**. They exist purely as a versioned backup of VS Code user-level configuration.

Distribution to other machines is handled by VS Code's built-in Settings Sync.

## Files

- `settings.json` — User-level editor settings
- `extensions.json` — Extension recommendations and unwanted list
