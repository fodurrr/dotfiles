# =============================================================================
# apps.toml - Centralized App Registry
# =============================================================================
# Every app, tool, and config is defined here with profile assignments.
#
# Fields:
#   type        = "cask" | "brew" | "mise" | "curl" | "stow"
#   name        = package name (optional, defaults to section name)
#   tap         = homebrew tap (optional, for casks from taps)
#   version     = version string (optional, for mise tools)
#   category    = grouping for documentation
#   profiles    = ["minimal", "standard", "developer", "hacker", "server"]
#   depends_on  = "app-key" (optional, install dependency first)
#   config_for  = "app-key" (optional, associate config/defaults to app)
#   platform    = ["macos", "linux"] (optional, defaults to all platforms)
#   linux_name  = Linux package name for cross-platform brew entries (optional)
#   linux_apt   = Debian/Ubuntu package override (optional)
#   linux_dnf   = Fedora/RHEL package override (optional)
#
# To add an app to a profile: add the profile name to the profiles array
# To create a new profile: just use a new name in any profiles array
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# TERMINALS
# ─────────────────────────────────────────────────────────────────────────────
[apps.ghostty]
type = "cask"
category = "terminals"
profiles = ["minimal", "standard", "developer", "hacker"]
platform = ["macos"]
description = "GPU-accelerated terminal emulator"

[apps.warp]
type = "cask"
category = "terminals"
profiles = ["developer"]
platform = ["macos"]
description = "AI-powered modern terminal"

# ─────────────────────────────────────────────────────────────────────────────
# EDITORS
# ─────────────────────────────────────────────────────────────────────────────
[apps.zed]
type = "cask"
category = "editors"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "High-performance code editor"

[apps.visual-studio-code]
type = "cask"
category = "editors"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "Extensible code editor"

[apps.antigravity]
type = "cask"
category = "editors"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "Note-taking app"

# [apps.neovim]
# type = "mise"
# category = "editors"
# profiles = ["hacker", "server"]
# description = "Vim-based text editor"

[apps.helix]
type = "mise"
category = "editors"
profiles = ["developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Post-modern modal text editor"

# ─────────────────────────────────────────────────────────────────────────────
# BROWSERS
# ─────────────────────────────────────────────────────────────────────────────
[apps.firefox]
type = "cask"
category = "browsers"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "Privacy-focused web browser"

[apps.google-chrome]
type = "cask"
category = "browsers"
profiles = ["minimal", "standard"]
platform = ["macos"]
description = "Google web browser"

[apps.microsoft-edge]
type = "cask"
category = "browsers"
profiles = ["standard"]
platform = ["macos"]
description = "Microsoft web browser"

# ─────────────────────────────────────────────────────────────────────────────
# AI TOOLS
# ─────────────────────────────────────────────────────────────────────────────
# Daily/Developer users: Desktop GUI apps
[apps.claude-desktop]
type = "cask"
name = "claude"
category = "ai"
profiles = ["standard", "developer"]
platform = ["macos"]
description = "Claude AI assistant (GUI)"

[apps.chatgpt-desktop]
type = "cask"
name = "chatgpt"
category = "ai"
profiles = ["standard", "developer"]
platform = ["macos"]
description = "ChatGPT AI assistant (GUI)"

[apps.codex-desktop]
type = "cask"
name = "codex"
category = "ai"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "OpenAI Codex assistant (GUI)"

[apps.opencode-desktop]
type = "cask"
category = "ai"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "OpenCode AI assistant (GUI)"

# Hacker users: CLI tools only
[apps.claude-cli]
type = "mise"
name = "claude"
version = "latest"
category = "ai"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Claude AI assistant (CLI)"

[apps.opencode-cli]
type = "mise"
name = "opencode"
version = "latest"
category = "ai"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "OpenCode AI assistant (CLI)"

[apps.codex-cli]
type = "mise"
name = "codex"
version = "latest"
category = "ai"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "OpenAI Codex assistant (CLI)"

[apps.codex-acp]
type = "brew"
category = "ai"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "Codex adapter for ACP-compatible clients (Zed integration)"

[apps.gemini-cli]
type = "mise"
category = "ai"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
depends_on = "node"
description = "Google Gemini AI (CLI)"

# ─────────────────────────────────────────────────────────────────────────────
# WINDOW MANAGEMENT
# ─────────────────────────────────────────────────────────────────────────────
[apps.aerospace]
type = "cask"
tap = "nikitabobko/tap"
name = "aerospace"
category = "window-management"
profiles = ["hacker"]
platform = ["macos"]
description = "i3-like tiling window manager"

[apps.sketchybar]
type = "brew"
tap = "FelixKratz/formulae"
category = "status-bar"
profiles = ["hacker"]
platform = ["macos"]
service = true
description = "Customizable macOS status bar"

[apps.sketchybar-system-stats]
type = "brew"
tap = "joncrangle/tap"
category = "status-bar"
profiles = ["hacker"]
platform = ["macos"]
depends_on = "sketchybar"
description = "Event-driven system stats for SketchyBar"

[apps.borders]
type = "brew"
tap = "FelixKratz/formulae"
category = "window-management"
profiles = ["hacker"]
platform = ["macos"]
description = "Active window border highlight"

[apps.hammerspoon]
type = "cask"
category = "window-management"
profiles = ["hacker"]
platform = ["macos"]
description = "Automation runtime for hover window border"

[apps.sketchybar-config]
type = "stow"
package = "sketchybar"
category = "config"
profiles = ["hacker"]
platform = ["macos"]
depends_on = "sketchybar"
config_for = "sketchybar"
description = "SketchyBar configuration"

# ─────────────────────────────────────────────────────────────────────────────
# PRODUCTIVITY
# ─────────────────────────────────────────────────────────────────────────────
[apps.raycast]
type = "cask"
category = "productivity"
profiles = ["minimal", "standard", "developer", "hacker"]
platform = ["macos"]
description = "Spotlight replacement launcher"

[apps.raycast-config]
type = "stow"
package = "raycast"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker"]
platform = ["macos"]
config_for = "raycast"
description = "Raycast scripts and settings"

[apps.obsidian]
type = "cask"
category = "productivity"
profiles = ["standard", "developer"]
platform = ["macos"]
description = "Markdown-based knowledge base"

[apps.tana]
type = "cask"
category = "productivity"
profiles = ["developer"]
platform = ["macos"]
description = "Knowledge management workspace with AI-powered outlining"

[apps.bitwarden]
type = "cask"
category = "productivity"
profiles = ["standard"]
platform = ["macos"]
description = "Open-source password manager"

[apps.aqua-voice]
type = "cask"
category = "productivity"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "Real-time voice dictation (Recommended)"

[apps.keycastr]
type = "cask"
category = "productivity"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "Keystroke visualizer for presentations"

[apps.bettershot]
type = "cask"
category = "productivity"
profiles = ["standard", "developer", "hacker"]
platform = ["macos"]
description = "Screenshot capture and editing tool"

[apps.kap]
type = "cask"
category = "productivity"
profiles = ["standard", "developer", "hacker"]
platform = ["macos"]
description = "Open-source screen recorder"

[apps.stretchly]
type = "cask"
category = "productivity"
profiles = ["standard", "developer", "hacker"]
platform = ["macos"]
description = "Break reminder and wellness app"

# Alternative dictation tools - uncomment if preferred over Aqua Voice
# [apps.superwhisper]
# type = "cask"
# category = "productivity"
# profiles = ["developer", "hacker"]
# description = "Offline AI dictation"

# [apps.wispr-flow]
# type = "cask"
# category = "productivity"
# profiles = ["developer", "hacker"]
# description = "AI voice assistant"

# ─────────────────────────────────────────────────────────────────────────────
# CLOUD STORAGE
# ─────────────────────────────────────────────────────────────────────────────
# [apps.onedrive]
# type = "cask"
# category = "cloud-storage"
# profiles = ["standard"]
# description = "Microsoft cloud storage"

[apps.google-drive]
type = "cask"
category = "cloud-storage"
profiles = ["standard"]
platform = ["macos"]
description = "Google cloud storage"

[apps.microsoft-office]
type = "cask"
category = "office"
profiles = ["standard"]
platform = ["macos"]
description = "Microsoft 365 Office suite"

# [apps.microsoft-teams]
# type = "cask"
# category = "communication"
# profiles = ["standard"]
# description = "Team collaboration"

# ─────────────────────────────────────────────────────────────────────────────
# MEDIA & COMMUNICATION
# ─────────────────────────────────────────────────────────────────────────────
[apps.discord]
type = "cask"
category = "media"
profiles = ["standard"]
platform = ["macos"]
description = "Voice and text chat platform"

[apps.spotify]
type = "cask"
category = "media"
profiles = ["standard"]
platform = ["macos"]
description = "Music streaming service"

[apps.vlc]
type = "cask"
category = "media"
profiles = ["standard"]
platform = ["macos"]
description = "Universal media player"

[apps.whatsapp]
type = "cask"
category = "communication"
profiles = ["standard"]
platform = ["macos"]
description = "Messaging app"

# ─────────────────────────────────────────────────────────────────────────────
# VIRTUALIZATION
# ─────────────────────────────────────────────────────────────────────────────
# [apps.orbstack]
# type = "cask"
# category = "virtualization"
# profiles = ["developer", "hacker"]
# description = "Fast Docker & Linux VMs"

# [apps.utm]
# type = "cask"
# category = "virtualization"
# profiles = ["developer", "hacker"]
# description = "Virtual machine manager"

# [apps.qemu]
# type = "brew"
# category = "virtualization"
# profiles = ["developer", "hacker"]
# description = "Machine emulator and virtualizer"

# ─────────────────────────────────────────────────────────────────────────────
# DATABASE
# ─────────────────────────────────────────────────────────────────────────────
# [apps.beekeeper-studio]
# type = "cask"
# category = "database"
# profiles = ["developer", "hacker"]
# description = "Open source SQL editor and database manager"

# [apps.pgadmin4]
# type = "cask"
# category = "database"
# profiles = ["developer", "hacker"]
# description = "Official PostgreSQL admin GUI"

# [apps.postgresql]
# type = "brew"
# name = "postgresql@17"
# category = "database"
# profiles = ["developer", "hacker"]
# service = true
# description = "PostgreSQL database server"

# ─────────────────────────────────────────────────────────────────────────────
# DISPLAY
# Note: Install only ONE of the following (BetterDisplay recommended)
# ─────────────────────────────────────────────────────────────────────────────
[apps.betterdisplay]
type = "cask"
category = "display"
profiles = ["standard", "developer", "hacker"]
platform = ["macos"]
description = "Advanced display management with virtual monitors"

# Alternative: simpler brightness control only (uncomment to use instead)
# [apps.monitorcontrol]
# type = "cask"
# category = "display"
# profiles = ["standard", "developer", "hacker"]
# description = "External display brightness (lighter alternative to BetterDisplay)"

# ─────────────────────────────────────────────────────────────────────────────
# UTILITIES
# ─────────────────────────────────────────────────────────────────────────────
# [apps.setapp]
# type = "cask"
# category = "utilities"
# profiles = ["standard"]
# description = "Mac app subscription service"

[apps.sandvault]
type = "brew"
category = "security"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "macOS sandbox for AI coding agents"

[apps.keymapp]
type = "cask"
category = "utilities"
profiles = ["hacker"]
platform = ["macos"]
description = "ZSA keyboard configuration"

[apps.pearcleaner]
type = "cask"
category = "utilities"
profiles = ["standard"]
platform = ["macos"]
description = "Uninstall apps and remove leftovers"

[apps.thaw]
type = "cask"
category = "utilities"
profiles = ["developer", "hacker"]
platform = ["macos"]
description = "Menu bar manager"

# ─────────────────────────────────────────────────────────────────────────────
# FONTS
# ─────────────────────────────────────────────────────────────────────────────
[apps.font-jetbrains-mono-nerd-font]
type = "cask"
category = "fonts"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos"]
description = "Developer font with icons"

# ─────────────────────────────────────────────────────────────────────────────
# CLI TOOLS (mise)
# ─────────────────────────────────────────────────────────────────────────────
[apps.starship]
type = "mise"
category = "cli"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Cross-shell prompt"

[apps.eza]
type = "mise"
category = "cli"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Modern ls replacement"

[apps.bat]
type = "mise"
category = "cli"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Cat with syntax highlighting"

[apps.ripgrep]
type = "mise"
category = "cli"
profiles = ["developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Fast recursive grep"

[apps.fzf]
type = "mise"
category = "cli"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Fuzzy finder"

[apps.sheldon]
type = "mise"
category = "cli"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Fast shell plugin manager"

[apps.jq]
type = "mise"
category = "cli"
profiles = ["developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "JSON processor"

[apps.gh]
type = "mise"
category = "cli"
profiles = ["developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "GitHub CLI"

[apps.pitchfork]
type = "mise"
name = "pitchfork"
version = "latest"
category = "cli"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Mise registry utility for release and changelog workflows"

[apps.tmux]
type = "mise"
category = "cli"
profiles = ["hacker", "server"]
platform = ["macos", "linux"]
description = "Terminal multiplexer"

[apps.yazi]
type = "mise"
category = "cli"
profiles = ["hacker", "server"]
platform = ["macos", "linux"]
description = "Terminal file manager"

[apps.btop]
type = "brew"
category = "cli"
profiles = ["hacker", "server"]
platform = ["macos", "linux"]
linux_name = "btop"
description = "Resource monitor"

[apps.ncdu]
type = "brew"
category = "cli"
profiles = ["hacker", "server"]
platform = ["macos", "linux"]
linux_name = "ncdu"
description = "Disk usage analyzer"

[apps.lazygit]
type = "mise"
category = "cli"
profiles = ["hacker", "server"]
platform = ["macos", "linux"]
description = "Terminal Git UI"

[apps.mole]
type = "brew"
tap = "tw93/tap"
category = "cli"
profiles = ["minimal", "standard", "developer", "hacker"]
platform = ["macos"]
description = "System cleanup and optimization CLI"

# ─────────────────────────────────────────────────────────────────────────────
# RUNTIMES (mise)
# ─────────────────────────────────────────────────────────────────────────────
[apps.node]
type = "mise"
version = "lts"
category = "runtimes"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "JavaScript runtime"

[apps.python]
type = "mise"
version = "3.14"
category = "runtimes"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Python runtime"

[apps.rust]
type = "mise"
version = "stable"
category = "runtimes"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Systems programming language"

[apps.bun]
type = "mise"
version = "1.3"
category = "runtimes"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Fast JavaScript runtime"

[apps.pnpm]
type = "mise"
version = "10.28"
category = "runtimes"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
depends_on = "node"
description = "Fast Node package manager"

[apps.erlang]
type = "mise"
version = "28.3"
category = "runtimes"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Concurrent programming runtime"

[apps.elixir]
type = "mise"
version = "1.19.5-otp-28"
category = "runtimes"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
depends_on = "erlang"
description = "Functional programming language"

# ─────────────────────────────────────────────────────────────────────────────
# STOW PACKAGES (configs)
# ─────────────────────────────────────────────────────────────────────────────
[apps.git-config]
type = "stow"
package = "git"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Git configuration"

[apps.zsh-config]
type = "stow"
package = "zsh"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Zsh shell configuration"

[apps.sheldon-config]
type = "stow"
package = "sheldon"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Sheldon plugin config"

[apps.starship-config]
type = "stow"
package = "starship"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
config_for = "starship"
description = "Starship prompt config"

[apps.ghostty-config]
type = "stow"
package = "ghostty"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker"]
platform = ["macos"]
config_for = "ghostty"
description = "Ghostty terminal config"

[apps.terminal-config]
type = "defaults"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos"]
description = "Terminal.app Catppuccin theme"

[apps.zed-config]
type = "stow"
package = "zed"
category = "config"
profiles = ["developer", "hacker"]
platform = ["macos"]
config_for = "zed"
description = "Zed editor config"

[apps.mise-config]
type = "stow"
package = "mise"
category = "config"
profiles = ["minimal", "standard", "developer", "hacker", "server"]
platform = ["macos", "linux"]
description = "Mise version manager config"

[apps.aerospace-config]
type = "stow"
package = "aerospace"
category = "config"
profiles = ["hacker"]
platform = ["macos"]
config_for = "aerospace"
description = "Aerospace WM config"

[apps.hammerspoon-config]
type = "stow"
package = "hammerspoon"
category = "config"
profiles = ["hacker"]
platform = ["macos"]
config_for = "hammerspoon"
description = "Hammerspoon hover-border config"

[apps.tmux-config]
type = "stow"
package = "tmux"
category = "config"
profiles = ["hacker", "server"]
platform = ["macos", "linux"]
config_for = "tmux"
description = "Tmux multiplexer config"

[apps.helix-config]
type = "stow"
package = "helix"
category = "config"
profiles = ["developer", "hacker", "server"]
platform = ["macos", "linux"]
config_for = "helix"
description = "Helix editor config"

[apps.yazi-config]
type = "stow"
package = "yazi"
category = "config"
profiles = ["hacker", "server"]
platform = ["macos", "linux"]
config_for = "yazi"
description = "Yazi file manager config"

[apps.cheatsheet-config]
type = "stow"
package = "cheatsheet"
category = "config"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Keyboard shortcuts cheatsheet"

[apps.keyboard-layout-config]
type = "stow"
package = "keyboard-layout"
category = "config"
profiles = ["developer", "hacker"]
platform = ["macos", "linux"]
description = "Voyager keyboard layout viewer"
